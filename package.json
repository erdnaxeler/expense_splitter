{
  "name": "expense-splitter",
  "version": "1.0.0",
  "private": true,
  "description": "Self-hosted Splitwise-style expense splitter with weighted groups. Zero dependencies, no paywalls.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
